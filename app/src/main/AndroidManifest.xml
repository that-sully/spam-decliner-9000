<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- =====================================================================
     PERMISSIONS
     ===================================================================== -->

    <!--
        READ_CALL_LOG: Let the app display blocked call history to the user.
        Requires runtime permission grant (dangerous permission).
    -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!--
        READ_PHONE_STATE: Access basic phone state info (e.g. is a call active).
        Requires runtime permission grant (dangerous permission).
    -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
        INTERNET: Required only if you add remote spam API lookups.
        Remove this if keeping the database entirely local.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        RECEIVE_BOOT_COMPLETED: Restart any background workers after device reboot.
        Needed if you schedule periodic DB updates with WorkManager.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
        NOTE: You do NOT need CALL_PHONE or ANSWER_PHONE_CALLS.
        CallScreeningService handles blocking without those permissions.
    -->

    <!-- =====================================================================
         FEATURE REQUIREMENTS
         ===================================================================== -->

    <!-- Declares that the app uses telephony — filters out non-phone devices on Play Store -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />

    <!-- =====================================================================
         APPLICATION
         ===================================================================== -->

    <application
        android:name=".SpamBlockerApp"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spamdecliner9000"
        tools:targetApi="31">
        <!-- =================================================================
             MAIN ACTIVITY
             ================================================================= -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Spamdecliner9000">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- =================================================================
             CALL SCREENING SERVICE  ← the core of the app
             ================================================================= -->
        <service
            android:name=".service.SpamCallScreeningService"
            android:exported="true"
            android:permission="android.permission.BIND_SCREENING_SERVICE">
            <!--
                BIND_SCREENING_SERVICE ensures only the Android telecom
                framework can bind to this service. DO NOT remove this permission.

                The intent filter name must be exactly:
                android.telecom.CallScreeningService
            -->
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- =================================================================
             BOOT RECEIVER (optional — for WorkManager DB sync on reboot)
             ================================================================= -->
        <!--
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        -->
    </application>

</manifest>